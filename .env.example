##############################################################################
# 환경 변수 설정 파일 (.env)
# 
# 사용법: 이 파일을 .env로 복사하고 실제 값으로 수정하세요
# cp .env.example .env
##############################################################################

#=============================================================================
# 외부 VPS 설정 (Reverse Tunneling)
#=============================================================================
# VPS 도메인 (HTTPS가 적용된 외부 도메인)
# Google OAuth Authorized Redirect URI에 등록할 주소
EXTERNAL_BASE_URL=https://your-domain.com

# VPS SSH 접속 정보
VPS_HOST=your-vps-ip-or-hostname
VPS_USER=ubuntu
VPS_SSH_PORT=22

#=============================================================================
# Google OAuth 2.0 설정
#=============================================================================
# Google Cloud Console에서 발급받은 클라이언트 정보
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

#=============================================================================
# JWT 및 Flask 보안 설정
#=============================================================================
# 프로덕션에서는 반드시 강력한 랜덤 문자열로 변경하세요
# 생성 명령: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this
FLASK_SECRET_KEY=your-flask-secret-key-change-this

#=============================================================================
# MongoDB 설정
#=============================================================================
# MongoDB Atlas 연결 문자열
# 27017 포트가 차단된 경우 터널링 후 localhost:27017 사용
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
MONGO_DB=soundroutine

# 27017 차단 시 터널링 설정 (VPS를 경유)
# 형식: localhost:27017:cluster-shard.mongodb.net:27017
# MONGO_TUNNEL_LOCAL=27017:cluster0-shard-00-00.xxxxx.mongodb.net:27017

#=============================================================================
# 애플리케이션 설정
#=============================================================================
SOUNDROUTINE_DB_BACKEND=mongodb
SOUNDROUTINE_DEFAULT_LIMIT=100
SOUNDROUTINE_LOG_LEVEL=INFO
